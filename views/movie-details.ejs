<%- include('partials/header', { title: movie.name, user: user }) %>

<div class="container page-container">
    <h1><%= movie.name %> (<%= movie.year %>)</h1>

    <div class="movie-details">
        <p><strong>Description:</strong> <%= movie.description %></p>
        <p><strong>Rating:</strong> <%= movie.rating %>/10</p>
        <p><strong>Duration:</strong> <%= movie.duration %> minutes</p>
        <p><strong>Genres:</strong> <%= movie.genres.join(', ') %></p>
        <p><strong>Added by:</strong> <%= movie.addedBy.username %></p>
    </div>

    <% if (isOwner) { %>
        <div class="movie-actions">
            <a href="/movies/<%= movie._id %>/edit" class="btn btn-primary">Edit Movie</a>
            <form action="/movies/<%= movie._id %>/delete"
                  method="POST"
                  class="inline-form">
                <button type="submit" class="btn btn-danger">Delete Movie</button>
            </form>
        </div>
    <% } %>

    <a href="/movies" class="btn btn-secondary">Back to Movies</a>
</div>

<%- include('partials/footer') %>
